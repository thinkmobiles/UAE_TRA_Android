<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/hex_primary_orange</item>

        <!-- colorPrimaryDark is used for the status bar -->
        <item name="colorPrimaryDark">@color/dark_orange</item>
        <item name="android:textColorPrimary">@color/text_color_white</item>

        <!-- colorAccent is used as the default value for colorControlActivated,
             which is used to tint widgets -->
        <item name="colorAccent">@color/color_accent</item>
    </style>


    <!--            AuthorizationActivity               -->
    <style name="AuthorizationFieldsSeparator">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">@dimen/dp_authorization_fields_separator_height</item>
        <item name="android:layout_marginLeft">@dimen/dp_authorization_fields_separator_margin</item>
        <item name="android:background">@color/hex_authorization_fields_separator_color</item>
    </style>

    <style name="TransparentGrayField">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:padding">@dimen/dp_transparent_fields_padding</item>
        <item name="android:background">@color/hex_authorization_fields_color</item>
        <item name="android:textColor">@color/hex_authorization_fields_text_color</item>
        <item name="android:textColorHint">@color/hex_authorization_fields_text_color</item>
    </style>

    <style name="TransparentWhiteButton">
        <item name="android:background">@drawable/selector_authorization_button</item>
        <item name="android:textSize">@dimen/dp_authorization_button_text_size</item>
        <item name="android:textColor">@color/hex_primary_orange</item>
    </style>

    <style name="BlurButtonStyle" parent="TransparentWhiteButton">
        <item name="android:layout_marginLeft">@dimen/dp_button_margin</item>
        <item name="android:layout_marginRight">@dimen/dp_button_margin</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">match_parent</item>
    </style>

</resources>
